services:
  # Flask Web Service - Debug Version
  - type: web
    name: trig-verifier
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python debug_app.py
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 8080
